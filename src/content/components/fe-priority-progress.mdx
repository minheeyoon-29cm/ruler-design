---
title: "Progress/FE PriorityProgress"
description: "Meta title=\"Progress/FE PriorityProgress\" /"
status: "draft"
version: "1.0.0"
category: "component"
tags: ["ui", "component"]
platforms: ["web", "ios", "android"]
updated: "2025-05-29"
---

# Ruler Priority Progress

[ **Status** ] ✅ : Tailwind / 📦 : Emotion / NONE : 미개발 컴포넌트

## Pritority 0

 item.priority === PRIORITY.P0)
    .map((item, index) => [
      index + 1,
      item.name,
      item.design,
      item.fe.includes(STATUS.TAILWIND_DONE) ? '✅' : item.fe.includes(STATUS.EMOTION_DONE) ? '📦' : 'NONE',
      item.feMD ?? '-',
      item.feEtc ?? '-',
    ])}
/>

수정필요: Table 컴포넌트 수동 변환 필요
```

          item.priority === PRIORITY.P0 && item.design === STATUS.DONE && item.fe.includes(STATUS.TAILWIND_TODO),
      ).length,
      allComponent
        .filter(
          (item) =>
            item.priority === PRIORITY.P0 && item.design === STATUS.DONE && item.fe.includes(STATUS.TAILWIND_TODO),
        )
        .reduce((acc, cur) => Number(acc) + (Number(cur?.feMD ?? 0) ?? 0), 0),
    ],
  ]}
/>
```

## Pritority 1

 item.priority === PRIORITY.P1)
    .map((item, index) => [
      index + 1,
      item.name,
      item.design,
      item.fe.includes(STATUS.TAILWIND_DONE) ? '✅' : item.fe.includes(STATUS.EMOTION_DONE) ? '📦' : 'NONE',
      item.feMD ?? '-',
      item.feEtc ?? '-',
    ])}
/>

수정필요: Table 컴포넌트 수동 변환 필요
```

          item.priority === PRIORITY.P1 && item.design === STATUS.DONE && item.fe.includes(STATUS.TAILWIND_TODO),
      ).length,
      allComponent
        .filter(
          (item) =>
            item.priority === PRIORITY.P1 && item.design === STATUS.DONE && item.fe.includes(STATUS.TAILWIND_TODO),
        )
        .reduce((acc, cur) => Number(acc) + (Number(cur?.feMD ?? 0) ?? 0), 0),
    ],
  ]}
/>
```

## Pritority 2

 item.priority === PRIORITY.P2)
    .map((item, index) => [
      index + 1,
      item.name,
      item.design,
      item.fe.includes(STATUS.TAILWIND_DONE) ? '✅' : item.fe.includes(STATUS.EMOTION_DONE) ? '📦' : 'NONE',
      item.feMD ?? '-',
      item.feEtc ?? '-',
    ])}
/>

## Pritority 3

 item.priority === PRIORITY.P3)
    .map((item, index) => [
      index + 1,
      item.name,
      item.design,
      item.fe.includes(STATUS.TAILWIND_DONE) ? '✅' : item.fe.includes(STATUS.EMOTION_DONE) ? '📦' : 'NONE',
      item.feMD ?? '-',
      item.feEtc ?? '-',
    ])}
/>## Props

| name | value |
| --- | --- |
| 속성명 | 값 |

## Color Spec

수정필요: Color Spec 섹션 추가 필요

## Size & Typography Spec

수정필요: Size Spec 섹션 추가 필요

## 상태

## Anatomy

![Progress/FE PriorityProgress Anatomy](/images/components/progress/fe priorityprogress/states/progress/fe priorityprogress-anatomy.jpg)

## Programmatic State

| State | Value | Default | Description |
| --- | --- | --- | --- |
| isDisabled | boolean | false | 비활성화 상태 |

## Behavior State

| State | Value | Default | Description |
| --- | --- | --- | --- |
| hover | boolean | false | 마우스 오버 상태 |
| pressed | boolean | false | 클릭/탭 상태 |
| focus | boolean | false | 키보드 포커스 상태 |

